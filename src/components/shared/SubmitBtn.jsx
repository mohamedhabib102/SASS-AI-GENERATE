import React from 'react'
// import { Button } from '../button'

const SubmitBtn = ({text}) => {
    return (
        <button>
            <span>{text}</span>
        </button>
    )
}

export default SubmitBtn